<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash + initial frame before Compose paints. Real palette comes from
         the Compose theme. Keep this minimal. -->
    <style name="Theme.PosTerminal" parent="android:Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>
</resources>
