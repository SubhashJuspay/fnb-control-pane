# Improvements Log

Running log of changes made to push the platform toward MVP.
Each entry: a brief description and the surfaces it touched.
Newest at the bottom.

## Customer-facing UI polish (2026-05-04)

| # | Change | Files |
|---|--------|-------|
| 1 | Menu landing hero (tenant name, currency badge, "Pickup • Pay on pickup" pills) | `apps/web/app/(public)/order/[tenantSlug]/[locationSlug]/page.tsx` |
| 2 | Multi-menu tabs + sticky section jump-nav | `apps/web/components/order/public-menu-list.tsx` |
| 3 | Item tile redesigned: image, dietary chips, in-cart badge, sold-out treatment, hover `+` affordance | `apps/web/components/order/public-menu-item-tile.tsx`, `public-modifier-picker.tsx` |
| 4 | Cart drawer: item count in title, continue-shopping button, friendlier empty state | `apps/web/components/order/cart-drawer.tsx` |
| 5 | Checkout: order summary card, multiline notes textarea, "Place order • $total" CTA, empty-cart redirect, scheduled-pickup placeholder removed | `apps/web/components/order/checkout-form.tsx` |
| 6 | Confirmation: prominent order #, what's-next timeline, styled tracking link with copy button | `apps/web/components/order/confirmation-card.tsx`, `confirmation/[token]/page.tsx` |
| 7 | Tracking: 4-step progress stepper, first-name greeting, "Updated Xs ago" indicator, broken-out subtotal/tax/total, rejected-state card | `apps/web/components/order/tracking-page.tsx` |

## Hardening (2026-05-06)

| # | Change | Why | Files |
|---|--------|-----|-------|
| 8 | Removed pre-migration extension SQL from the Postgres init dir | The init scripts created `pgcrypto`/`pg_trgm`/`citext` before Prisma ran, which made `prisma migrate dev` see "drift" on a fresh DB. The first migration already declares the extensions with `CREATE EXTENSION IF NOT EXISTS`, so leaving extension creation to migrations alone fixes the drift. `pnpm db:migrate` now works on a clean stack as the README claims. | `docker/postgres-init/01-extensions.sql` (deleted), `02-citext.sql` (deleted), `docker/postgres-init/README.md` (added) |
| 9 | E2E `resetTestData()` now drops staff/scheduling rows before users | Restrict FKs on `Shift.userId`, `Shift.createdById`, `TimeEntry.userId`, `EmploymentProfile.userId`, `AvailabilityWindow.userId` made the user-wipe fail whenever a prior `pnpm db:seed:demo` had been run. Now the suite tolerates demo data as a starting point. | `apps/web/tests/e2e/fixtures/seed.ts` |
| 10 | Cart drawer: SPA navigation to checkout via `router.push` | Replaces the full-document `window.location.href` reload, keeping the cart provider hot and avoiding a flash. Drawer also closes on click for a smoother transition. | `apps/web/components/order/cart-drawer.tsx` |
| 11 | Allergen tags on the public menu | API `PublicMenuItem` now exposes `allergenTags` (already present in DB + authenticated `MenuItem`); the web tile renders them as red chips alongside the green dietary chips with a `title` of "Contains X" for accessibility. Demo seed already populates allergens for the relevant items. | `apps/api/src/schema/public-location.ts`, `apps/web/lib/graphql/operations/online-orders.graphql`, `apps/web/lib/graphql/generated/*` (regenerated), `apps/web/components/order/public-menu-item-tile.tsx`, `apps/web/components/order/public-modifier-picker.tsx` |
| 12 | Address / phone / opening hours on Location | New Prisma columns `phone`, `opening_hours` (existing `address` was already there); migration `20260506104603_location_contact_info`. Public projection exposes them. New `<LocationHero>` renders the address (linked to Google Maps), `tel:` link, today's hours, and a live Open/Closed badge derived from `Intl.DateTimeFormat` so the badge respects the location's IANA timezone with no extra deps. Demo seed populates realistic data for all three locations. | `packages/db/prisma/schema.prisma`, `packages/db/prisma/migrations/20260506104603_location_contact_info/`, `packages/db/src/seed-demo.ts`, `apps/api/src/schema/public-location.ts`, `apps/web/lib/graphql/operations/online-orders.graphql`, `apps/web/lib/location-hours.ts` (new), `apps/web/components/order/location-hero.tsx` (new), `apps/web/app/(public)/order/[tenantSlug]/[locationSlug]/page.tsx` |
| 13 | Closed-state guard on the public order surface | Opening-hours helpers moved to `@repo/types` so both the web SSR and the API mutation share one definition of "open right now". The `submitOnlineOrder` resolver now rejects with a `ConflictError` when the location is closed; the menu page renders a prominent rose banner; the cart drawer disables the Checkout button with an inline notice; the checkout form refuses to submit. E2E fixture clears `openingHours` so the time-of-day-independent flow keeps passing. | `packages/types/src/opening-hours.ts` (new), `packages/types/src/index.ts`, `apps/web/lib/location-hours.ts` (now a re-export), `apps/api/src/schema/mutations/online-orders/submit-online-order.ts`, `apps/web/components/order/order-shell.tsx`, `apps/web/components/order/cart-drawer.tsx`, `apps/web/components/order/checkout-form.tsx`, `apps/web/app/(public)/order/[tenantSlug]/[locationSlug]/page.tsx`, `.../checkout/page.tsx`, `apps/web/tests/e2e/fixtures/seed.ts` |
| 14 | Transactional email for online orders | Adds four templates (received / confirmed / ready / rejected) rendered via the existing nodemailer transport. `submitOnlineOrder`, `confirmOnlineOrder`, `rejectOnlineOrder`, and `markTicketItemReady` each fire-and-forget the right one when the customer left an email. Failures are logged at `warn` level so SMTP outages never break the user-facing mutation. The "ready" email triggers exactly once, the moment the last non-voided item on a CONFIRMED ONLINE ticket reaches READY/SERVED. Verified end-to-end against MailHog. | `apps/api/src/email/online-order.ts` (new), `apps/api/src/schema/mutations/online-orders/submit-online-order.ts`, `confirm-online-order.ts`, `reject-online-order.ts`, `apps/api/src/schema/mutations/pos/mark-ticket-item-ready.ts` |
| 15 | Printable receipt for online orders | `OnlineOrderTracking` projection extended with full line items + tenant/location identity. New public route `/order/<tenant>/<location>/receipt/<token>` renders a print-friendly card with item lines, modifier breakdown, subtotal/tax/total, and a Print button (auto-prints when `?print=1`). The tracking page now links to it. Existing tests updated to the new projection shape. | `apps/api/src/schema/online-order-request.ts`, `online-order-request.test.ts`, `apps/web/lib/graphql/operations/online-orders.graphql`, `apps/web/components/order/receipt-card.tsx` (new), `apps/web/app/(public)/order/[tenantSlug]/[locationSlug]/receipt/[token]/page.tsx` (new), `apps/web/components/order/tracking-page.tsx`, `apps/web/app/(public)/order/[tenantSlug]/[locationSlug]/track/[token]/page.tsx` |
| 16 | SMS notifications for online orders (Twilio) | New `apps/api/src/sms/client.ts` posts to Twilio's REST API directly via `fetch` — no extra dep. Strictly opt-in: `TWILIO_ACCOUNT_SID` / `TWILIO_AUTH_TOKEN` / `TWILIO_FROM_NUMBER` must all be set or every send is a no-op. Concise SMS templates dispatch alongside the existing email on submit / confirm / ready / reject; failures are logged at `warn` so SMS outages never break the user-facing mutation. | `apps/api/src/env.ts`, `apps/api/src/sms/client.ts` (new), `apps/api/src/sms/online-order.ts` (new), `apps/api/src/email/online-order.ts` (now also exposes `customerPhone`), `submit-online-order.ts`, `confirm-online-order.ts`, `reject-online-order.ts`, `mark-ticket-item-ready.ts`, `.env.example` |
| 17 | Customer reservation booking widget | New public mutation `submitReservationRequest` (rate-limited 5/min per IP, validates against opening hours and a 90-day forward window). New public route `/book/<tenant>/<location>` with date/time/party-size/contact form. Server attributes the row to the tenant's existing system user so anonymous flow respects the `Reservation.createdById` Restrict FK without changing the schema. Lands as PENDING in the existing staff console. | `packages/validation/src/reservation.ts`, `apps/api/src/schema/mutations/reservations/submit-reservation-request.ts` (new), `apps/api/src/schema/mutations/reservations/inputs.ts`, `apps/api/src/schema/mutations/reservations/index.ts`, `apps/web/lib/graphql/operations/booking.graphql` (new), `apps/web/components/booking/booking-form.tsx` (new), `apps/web/components/booking/booking-shell.tsx` (new), `apps/web/app/(public)/book/[tenantSlug]/[locationSlug]/page.tsx` (new) |
| 18 | Public tenant signup / self-serve onboarding | New public mutation `signUpTenant` creates Tenant + Location + Owner User + OWNER Membership atomically (Prisma `$transaction`). Slug + email collisions return friendly errors. Rate-limited 3/min per IP. New `/sign-up` route renders the form (auto-slugifies the tenant slug from the name); the server action signs the user in immediately and redirects them to their new dashboard. Demo seed unchanged. | `packages/validation/src/auth.ts`, `apps/api/src/schema/mutations/sign-up-tenant.ts` (new), `apps/api/src/schema/index.ts`, `apps/web/lib/graphql/operations/signup.graphql` (new), `apps/web/app/(auth)/sign-up/page.tsx` (new — was a sub-route only), `apps/web/app/(auth)/sign-up/sign-up-tenant-form.tsx` (new), `apps/web/app/(auth)/sign-up/actions.ts` (new) |

## Clover-parity (2026-05-07)

| # | Change | Why | Files |
|---|--------|-----|-------|
| 19 | Tipping at close | New `tipCents` column on `Ticket` (migration `20260507_ticket_tip_cents`). `closeTicket` accepts an optional `tipCents`; `reopenTicket` clears it back to 0. POS close dialog gains a quick-pick row (No tip / 15% / 18% / 20%) plus a custom dollar field, computed against `totalCents`. Submit button shows the grand total live. Receipt projection (`OnlineOrderTracking`) and the customer-facing receipt page both surface a Tip + Grand total line when non-zero. | `packages/db/prisma/schema.prisma`, `packages/db/prisma/migrations/20260507_ticket_tip_cents/`, `packages/validation/src/ticket.ts`, `apps/api/src/schema/mutations/pos/inputs.ts`, `apps/api/src/schema/mutations/pos/close-ticket.ts`, `apps/api/src/schema/mutations/pos/reopen-ticket.ts`, `apps/api/src/schema/ticket.ts`, `apps/api/src/schema/online-order-request.ts`, `apps/api/src/schema/online-order-request.test.ts`, `apps/web/lib/graphql/operations/online-orders.graphql`, `apps/web/components/pos/close-ticket-dialog.tsx`, `apps/web/components/pos/active-ticket-panel.tsx`, `apps/web/components/order/receipt-card.tsx` |
| 20 | Refunds / closed-ticket adjustments | Manager-scope `refundTicket(ticketId, amountCents, reason)`. Additive: multiple partial refunds allowed, capped at `totalCents + tipCents`. New columns on `Ticket`: `refundCents`, `refundReason`, `refundedAt`, `refundedById` (User Restrict FK + relation `TicketRefundedBy`). New refund dialog on the closed-ticket detail page surfaces refundable + already-refunded + remaining; the totals panel grows a Tip / Refunded / Net rollup. Audit row records every partial refund. | `packages/db/prisma/schema.prisma`, `packages/db/prisma/migrations/20260507112226_ticket_refund_columns/`, `packages/validation/src/ticket.ts`, `apps/api/src/schema/mutations/pos/inputs.ts`, `apps/api/src/schema/mutations/pos/refund-ticket.ts` (new), `apps/api/src/schema/mutations/pos/index.ts`, `apps/api/src/schema/ticket.ts`, `apps/web/lib/graphql/operations/pos.graphql`, `apps/web/components/tickets/refund-ticket-dialog.tsx` (new), `apps/web/components/tickets/ticket-detail.tsx` |
| 21 | Day-end Z report | New `/end-of-day` route per location, with a date picker (defaults to today, capped at today). Renders the same data as the dashboard but as a single print-friendly sheet: summary (gross / discount / tax / net / **tips** / **refunds** / net-to-bank), sales-by-hour table, top items by quantity, server performance breakdown. The `salesSummary` aggregation now sums tips + refunds; insights tabs already query the new fields. New sidebar entry "End of day" sits below Insights. | `apps/api/src/analytics/summary.ts`, `apps/api/src/analytics/summary.test.ts`, `apps/api/src/schema/analytics.ts`, `apps/web/lib/graphql/operations/analytics.graphql`, `apps/web/components/analytics/end-of-day-report.tsx` (new), `apps/web/app/(app)/[tenantSlug]/[locationSlug]/end-of-day/page.tsx` (new), `apps/web/components/shell/sidebar.tsx` |
| 22 | CSV export on every insights tab | New `<DownloadCsvButton>` builds a UTF-8 BOM CSV from the same rows the table is rendering — no new resolver, no extra query. Wired into Sales, Items, Hours, Servers, and Guests reports. RFC-4180 quoting + date-stamped filenames. | `apps/web/components/analytics/download-csv.tsx` (new), `apps/web/components/analytics/sales-report.tsx`, `items-report.tsx`, `hours-report.tsx`, `servers-report.tsx`, `guests-report.tsx` |
| 23 | Inventory tracking (basic) | New `LocationItem.stockOnHand` + `lowStockThreshold` columns (migration `20260507113725_location_item_inventory`); both NULL means untracked. `closeTicket` decrements on-hand by quantity per non-voided line, swallowing failures so a stock race never blocks closure. `upsertLocationItem` accepts both fields and the location-overrides dialog grew a "Track inventory" toggle with stock + low-stock-threshold inputs. Pothos `LocationItem` exposes the new fields. Also fixes a pre-existing env brittleness: `OTEL_EXPORTER_OTLP_ENDPOINT=` and `SENTRY_DSN=` (empty strings) used to fail Zod URL validation on import; they now coerce to undefined. | `packages/db/prisma/schema.prisma`, `packages/db/prisma/migrations/20260507113725_location_item_inventory/`, `packages/validation/src/menu.ts`, `apps/api/src/env.ts`, `apps/api/src/schema/menu-item.ts`, `apps/api/src/schema/mutations/location-overrides/inputs.ts`, `apps/api/src/schema/mutations/location-overrides/upsert-location-item.ts`, `apps/api/src/schema/mutations/pos/close-ticket.ts`, `apps/api/src/schema/mutations/pos/reopen-ticket.test.ts`, `apps/api/src/schema/mutations/pos/mark-ticket-item-ready.test.ts`, `apps/web/lib/graphql/operations/menu.graphql`, `apps/web/components/menu/add-override-dialog.tsx` |
| 24 | Loyalty points (1 pt / $) | New `Guest.pointsBalance` (lifetime balance) + `Ticket.pointsEarned` columns (migration `20260507115706_loyalty_points`). `closeTicket` computes `floor((subtotal − discount) / 100)` per close (tax + tip excluded — matches operator expectations) and the new `accrueGuestPointsAfterClose` post-commit hook atomically increments the linked guest's balance via Prisma `increment`. Both fields exposed on the GraphQL `Guest` and `Ticket` types; the guest profile page grew a 4th KPI tile showing the live points total. | `packages/db/prisma/schema.prisma`, `packages/db/prisma/migrations/20260507115706_loyalty_points/`, `apps/api/src/guest/post-close.ts`, `apps/api/src/schema/guest.ts`, `apps/api/src/schema/ticket.ts`, `apps/api/src/schema/mutations/pos/close-ticket.ts`, `apps/web/lib/graphql/operations/guests.graphql`, `apps/web/components/guests/guest-detail.tsx` |

## Verification + deploy prep (2026-05-08)

| # | Change | Why | Files |
|---|--------|-----|-------|
| 25 | Typecheck cleanup across the workspace | `verbatimModuleSyntax` on Prisma 7-style strict builds was rejecting our `import { Prisma, prisma } from '@repo/db'`. Root cause: `packages/db/src/index.ts` had a redundant `export type { Prisma }` line that shadowed the value re-export from `export *`. Removed the redundant line so both runtime values (`Prisma.DbNull`) and types (`Prisma.InputJsonValue`) come through. Also fixed a duplicate `TicketStatus` declaration in `analytics/summary.ts` introduced when extending the row shape, dropped a Pothos/Zod input mismatch on `submitReservationRequestSchema.guestEmail` (the preprocess made the input type `unknown`, breaking the validate plugin), and tightened nullable coalesces in `end-of-day-report.tsx` + `receipt-card.tsx`. Workspace `pnpm typecheck` is green. | `packages/db/src/index.ts`, `apps/api/src/analytics/summary.ts`, `packages/validation/src/reservation.ts`, `apps/web/components/order/receipt-card.tsx`, `apps/web/components/analytics/end-of-day-report.tsx` |
| 26 | Test suite cleanup after the new email/SMS dispatch path | `confirmOnlineOrder`, `rejectOnlineOrder`, and `markTicketItemReady` now call `loadOrderEmailContext` post-mutation, which reads `prisma.onlineOrderRequest.findUnique`. Existing tests didn't mock that, so the post-update branch crashed. Added a default `mockFindUnique.mockResolvedValue(null)` so the notification side-effect is a no-op unless a test opts into it. Same treatment on `mark-ticket-item-ready.test.ts` for `prisma.ticket.findUnique`. Refreshed the `public-location.test.ts` happy-path expectation to include the new address/phone/openingHours fields. 892/892 unit + integration tests pass. | `apps/api/src/schema/mutations/online-orders/confirm-online-order.test.ts`, `reject-online-order.test.ts`, `apps/api/src/schema/mutations/pos/mark-ticket-item-ready.test.ts`, `apps/api/src/schema/public-location.test.ts`, `apps/api/src/analytics/summary.test.ts` |
| 27 | Fix E2E close-button regex | The tipping change made the close-ticket dialog button read `Close ticket • $X.XX`. `pos-flow.spec.ts` was matching by `name: /^close ticket$/i` which no longer matches. Switched to the existing `data-testid="close-ticket-submit"`. 15/16 E2E specs pass cleanly; the one outlier (`staff-flow`) is pre-existing calendar brittleness when "today" is Friday, not a regression. | `apps/web/tests/e2e/pos-flow.spec.ts` |
| 28 | Auto-snapshot the GraphQL SDL during codegen | Vercel runs the production `pnpm build` with no live API reachable — it has to read the committed `apps/web/lib/graphql/schema.graphql` snapshot. We were generating types against the live API but never updating the snapshot, so the build crashed on every new field added in this session (`tipCents`, `refundCents`, `pointsBalance`, `allergenTags`, `openingHours`, `OnlineOrderTracking.items`, etc.). Added the `@graphql-codegen/schema-ast` plugin so `pnpm codegen` writes the SDL alongside the TS types in one pass. Production build now compiles cleanly without API access. | `apps/web/codegen.ts`, `apps/web/package.json`, `apps/web/lib/graphql/schema.graphql` (regenerated) |
| 29 | Deployment plan | `docs/DEPLOYMENT.md` lays out three real options for "Vercel web + local backend": two ngrok tunnels, ngrok + Neon (recommended for an internal demo), or one ngrok + an Auth.js refactor to drop the direct Prisma dependency. Includes the "outgrowing this setup" thresholds and a daily-driver shortcuts section. | `docs/DEPLOYMENT.md` |
